# ── Server ─────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development          # development | production

# ── Mode ───────────────────────────────────────────────────────────────────
# Set to "true" to use seeded demo data without real API keys
DEMO_MODE=true

# ── LLM provider (OpenRouter) ──────────────────────────────────────────────
# Required for real AI analysis. All 5 selectable models route through here.
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ── STT provider (OpenAI Whisper) ──────────────────────────────────────────
# Required for real speech-to-text transcription (model: whisper-1).
OPENAI_API_KEY=your_openai_api_key_here

# ── CORS ───────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins. Leave empty to allow all (dev only).
# Example: CORS_ORIGINS=https://app.collectiq.in,https://staging.collectiq.in
CORS_ORIGINS=

# ── API versioning ─────────────────────────────────────────────────────────
# These are set automatically from defaults; override only if needed.
# API_PREFIX=/api
# API_V1_PREFIX=/api/v1
